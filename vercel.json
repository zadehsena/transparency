{
    "crons": [
        {
            "path": "/api/sync-jobs",
            "schedule": "0 6 * * *",
            "headers": {
                "Authorization": "Bearer ${CRON_SECRET}"
            }
        }
    ]
}